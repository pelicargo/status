# Status Reporter

A small service specifically for us. Too simple to be anything other than public domain.

Simple daemon that reports the branch, active docker containers, and if the repo has been modified any.

Install via "./install.sh"
